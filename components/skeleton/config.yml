files:
  abstract: resources/views/components/ui/skeleton/abstract.blade.php
  index: resources/views/components/ui/skeleton/index.blade.php
  text: resources/views/components/ui/skeleton/text.blade.php
mcp:
  name: skeleton
  title: Skeleton
  summary: Render animated placeholders matching content while data loads.
  category: feedback
  tags: [skeleton, loading, placeholder, shimmer]
  capabilities: [show-loading-placeholder]
  access: free
  install: skeleton
  root_tag: x-ui.skeleton
  child_tags: [x-ui.skeleton.text]
  props:
    animate: {type: string, required: false, default: glow, values: [pulse, wave, glow, none]}
    speed: {type: string, required: false, default: normal}
    size: {type: string, required: false, default: base}
  slots: []
  bindings: []
  events: []
  relationships: {requires_parent: null, allows_children: []}
  examples:
    minimal: '<x-ui.skeleton class="h-4 w-full" />'
    common: '<x-ui.skeleton animate="pulse" class="h-8 w-80" />'
    advanced: '<div class="space-y-2"><x-ui.skeleton class="h-6 w-1/3" /><x-ui.skeleton.text /></div>'
  docs: docs/components/skeleton/usage.md
  mistakes: [Shape skeletons to resemble the content they replace.]
